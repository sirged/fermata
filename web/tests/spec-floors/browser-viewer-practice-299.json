{
  "file": "tests/browser/viewer-practice.spec.js",
  "count": 1,
  "reason": "issue #299: the library page must ask GET /api/scores for its recent/neglected windows with the browser's own today, not silently fall back to the server's UTC one."
}
