{
  "file": "tests/browser/score-editor-native-262.spec.js",
  "count": 5,
  "reason": "issue #262 editing a score that IS notation rather than a transcription of one. Against the REAL server and a REAL uploaded file (the editor fixture uploaded through /api/upload, so there is an actual path under the throwaway library to hash), three tests: a native MusicXML score with no transcription row at all opens in the note editor on the file's own document (data-editor-available true, eight sounding notes, ordinal 3 reading string 1 fret 5 from fixtures/editor-score.js's table) and the panel says the file is what is showing; a fret change saves through the existing PUT as a source='edited' row that the real GET reads back (carrying <fret>7</fret>, every octave inside MusicXML's 0-9), after which the viewer draws the EDIT rather than the file and still does after a full page reload - while the library file is byte-identical on disk, its SHA-256 read straight off the filesystem before and after and its original text unchanged; and revert deletes the edited row (the server's 404 for a score with no extraction underneath being the success case), brings the file's own fret back on screen, leaves no revert control, and leaves the file's hash untouched throughout. Plus the #189 fuzz guard pointed at a native score: the SAME seeded driver (fixtures/editor-fuzz-driver.js, shared with score-editor-fuzz-189.spec.js rather than copied) at the same seed and N, over the SAME polyphonic document uploaded as a .musicxml file instead of served as a transcription row - so the whole-model audit is shown to hold on the real byte path (fetch, decode, import), and, on that same input, to go RED and name the note when an independent stale-render divergence is planted, then green again once a real edit reloads the view."
}
