{
  "file": "tests/browser/zzzzzz-score-metadata.spec.js",
  "count": 14,
  "reason": "issue #8: setting a difficulty from the score view writes it through a real PATCH and survives a reload, an explicit clear writes null rather than zero or the empty string, and the library's difficulty and key filters (including key=0, the falsy-value trap) narrow a real grid to the exact match; first review round adds the tempo control's own three cases (an out-of-range value refused with no uncaught page error, a fractional value rounded and saved in one PATCH, an ordinary value sent in exactly one PATCH) and the score view's controls row checked for clipping and page overflow at five widths plus real-click reachability at two, alongside the library filter row's own equivalent checks added to toolbar-responsive.spec.js."
}
