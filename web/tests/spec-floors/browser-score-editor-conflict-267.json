{
  "file": "tests/browser/score-editor-conflict-267.spec.js",
  "count": 4,
  "reason": "issue #267 two pages open on the same score, against the REAL server and a REAL uploaded file that already carries a hand edit, so both pages load the same stored row before either saves. Four tests: A and B both load, A saves and B saves what it loaded - B's own panel says the score changed somewhere else and that nothing was saved, offers loading what is stored or saving mine over it, still holds B's unsaved work (editor open, the note still reading 9), while the row read back from the server holds A's fret 7 and not B's 9, and A can still save again on top of its own row (so the refusal touched neither the content nor updated_at); one page saving three times in a row, each save carrying the value its OWN previous save returned rather than the one the page loaded, all three landing; the conflict's \"load what is stored\" replacing B's working document with A's version (fret 7 where B had typed 9, the renderer and the document agreeing about it) after which B can save on top of what it loaded; and the conflict's \"save mine over it\" reading what is stored now and replacing it with B's version deliberately, after which B holds the new stamp and its next save is not refused by its own. The two saves in each conflict test land well inside one second, so a per-second updated_at would make B's stale value match and every conflict assertion here go green when it must be red."
}
