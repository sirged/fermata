[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fermata"
version = "0.1.0"
description = "Self-hosted sheet music server"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pymupdf>=1.24",
    "python-multipart>=0.0.9",
]

[tool.hatch.build.targets.wheel]
packages = ["fermata"]
